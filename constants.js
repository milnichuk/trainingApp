import { Dimensions } from "react-native";

export const win = Dimensions.get('window');
export const w = win.width;
export const h = win.height;
export const mainBackground = '#FFEFD4'; // фон всех экранов